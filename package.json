{
  "name": "najah",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "install": "cd backend && npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps --include=dev",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "youtube-transcript": "^1.3.0"
  }
}
