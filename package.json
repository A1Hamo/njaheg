{
  "name": "najah",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build"
  }
}
